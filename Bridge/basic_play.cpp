#include "basic_play.h"

card::suit_type play_card(player)
{
    int card;
    cout<<"Pick a card"<<endl;

    card::suit_type;
    return
}

void start_play(card::suit_type trump, int bid, direction declarer, player* players)
{
    int min_tricks = bid + THE_BOOK;
    int def_tricks = 0;
    int dec_tricks = 0;
    direction lead = declarer;

    while(def_tricks < 13 - min_tricks)
    {
        play_card(players[lead]);

        while(true)
        {

        }
    }

}
